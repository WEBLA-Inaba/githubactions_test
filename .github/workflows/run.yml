name: learn-github-actions
on:
  push:
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - name: test echo
        run: echo "this is pen"
      - name: Checkout@v3
        uses: actions/checkout@v3
        - run: |
            ls -al
            cd .github/workflows/
            ls -al